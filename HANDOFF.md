# وثيقة التسليم — مدونة datarecovery-sa.com

> **الحالة:** المدونة مبنية ومُختبَرة محليًّا، وغير منشورة. كل ما تبقّى صلاحيات
> وحسابات لا يستطيع أحد إنجازها إلا مالك الحسابات.
>
> **آخر تحديث:** 2026-08-14 · **الأساس:** مكيَّفة من مدونة مشروع شقيق مثبتة بـ39 التزامًا.

---

## 1. الخريطة

### الحسابات

| الحساب | الدور | ملاحظة |
|---|---|---|
| `01team9639-maker` | يملك مستودع الموقع | `01team9639-maker/datarecovery-sa` — **موجود وعام** |
| `zeroone01z21-alt` | سيملك مستودعات المدونة | نفس الحساب المستخدم في المشروع الشقيق |
| الكاتب | يكتب المقالات | **لم يُحدَّد بعد** — يحتاج حساب GitHub وصلاحية Write على مستودع المحتوى وحده |
| Cloudflare | وسيط دخول اللوحة | الـWorker القائم يكفي للمشروعين معًا |
| Hostinger | الاستضافة | نفس الخطة إن كان النطاقان عليها |

### المستودعات

| المستودع | المحتوى | الحالة |
|---|---|---|
| `zeroone01z21-alt/datarecovery-blog-build` | القوالب والأدوات والضوابط (هذا المجلد) | ✅ منشأ ومدفوع |
| `zeroone01z21-alt/datarecovery-blog-content` | مقالات الكاتب وصورها | ✅ منشأ ومدفوع |
| `01team9639-maker/datarecovery-sa` | الموقع + `blog/` المولَّد | موجود وعام |

**لماذا الفصل:** الكاتب يحتاج صلاحية الكتابة على المحتوى، ولا يجوز أن يملك
صلاحية تعديل القوالب أو الميزانيات أو سير العمل. الفصل يجعل ذلك بنيويًّا لا
اعتمادًا على الانضباط.

### المسارات

| | العربية | الإنجليزية |
|---|---|---|
| الرئيسية | `/blog/` | `/blog/en/` |
| المقال | `/blog/{slug}/` | `/blog/en/{slug}/` |
| التصنيف | `/blog/categories/{slug}/` | `/blog/en/categories/{slug}/` |
| RSS | `/blog/index.xml` | `/blog/en/index.xml` |

العربية بلا بادئة لأن الموقع نفسه عربي على جذر النطاق. هذا **معكوس** عن
المدونة المرجعية، وانعكاسه لامس فاحص SEO وفاحص الفهرسة ومطبّع الخرائط
وفاحص الميزانيات — لا ملف الإعداد وحده.

---

## 2. ما أُنجز — وكيف تُثبته بنفسك

كل بند أدناه مُتحقَّق منه ببناء فعلي، لا بمراجعة نظر.

| البند | الإثبات |
|---|---|
| المدونة تبني | `hugo` → 20 صفحة عربية · 19 إنجليزية |
| الفحوصات السبعة تمرّ | `check_controls · check_content · check_seo · check_indexing · check_budgets · check_cms · check_deploy_scope` |
| الميزانية ضمن الحد | أثقل مسار حرج 137.6 من 220 KB |
| الهوية موحّدة مع الموقع | الهيكل يُصدَّر من `build/emit-chrome.js` في مستودع الموقع |
| المقال يرتبط بكيان الموقع | `Article` يشير إلى `datarecovery-sa.com/#business` نفسه |
| التصنيفات من خدماتنا | 6 تصنيفات مضبوطة من `controls/schema.json` |

### كيف تشغّل التحقق كاملًا

```bash
cd ~/Downloads/datarecovery-blog-build
python3 tools/prepare_content.py ~/Downloads/datarecovery-blog-content/content
python3 tools/check_content.py
hugo --gc --minify --destination public
python3 tools/normalize_sitemaps.py public
python3 tools/check_seo.py public
python3 tools/check_indexing.py public
python3 tools/check_budgets.py
python3 tools/check_controls.py
```

### فروق مقصودة عن المدونة المرجعية

- **الهيكل يُصدَّر لا يُكشط.** صفحات هذا الموقع مولّدة، فالهيكل يُقرأ من المولّد
  مباشرة عبر `build/emit-chrome.js`. لا تحليل HTML ولا عدّ وسوم `<div>`.
- **`main.js` وحده يُحمَّل.** `bootstrap.js` يضيف `.js` الذي يُخفي الفوتر
  (`opacity: 0`) بانتظار GSAP. المدونة بلا طبقة حركة، فتحميله يخفي الفوتر
  نهائيًّا. استبعاده يوفّر أيضًا نحو 115 KB.
- **`/blog/` مستثنى من خريطة تحويلات الموقع.** بلا ذلك يُخطف
  `/blog/what-is-raid/` إلى صفحة خدمة RAID.

---

## 3. ⛔ أخطاء لا تُكرَّر

### أ) الاستضافة

**`Password Protect Directories` في hPanel أسقطت خادم الويب كله** (2026-08-03).
الأعراض: TLS يُقطع، خطأ 521 من الخارج، بينما FTP وSSH يعملان. عاد فورًا عند
إزالة الحماية. **لا تُستخدم الأداة إطلاقًا.** البديل المطبَّق: `.htaccess`
يدوي داخل مجلد فرعي عبر حساب FTP محصور فيه — أسوأ أثره خطأ في ذلك المجلد.

**`Header` تحتاج `mod_headers` وهي غير مؤكّدة على هذا الخادم.** البديل الآمن:
`<meta name="robots" content="noindex">` داخل الصفحة.

**مزامنة Git تحذف ما ليس في المستودع.** في 2026-08-04 محا نشرٌ للموقع مجلد
`/blog/` كاملًا وكان يُرفع بـFTP. **هذا سبب أن تُنشر المدونة داخل مستودع الموقع
لا عبر FTP.**

**حذف `.htaccess` الجذر ضرره صامت:** الموقع يبدو سليمًا بينما تهبط التحويلات
وتنكشف مجلدات. الاستعادة: عدّل الملف في Git وادفع — المزامنة تكتبه.

### ب) القياس والتشخيص

**تحقّق من أداة القياس قبل تصديق نتيجتها.** `/dev/tcp` معطوب في بيئة التنفيذ
وفشل حتى على منفذ 443 العامل، فأنتج تشخيص «حجب IP» وهميًّا. **`nc` و`curl` صحيحان.**

**للتمييز بين «الموقع معطّل» و«عنواني محجوب»:** استخدم وسيطًا خارجيًّا مثل
`https://api.codetabs.com/v1/proxy?quest=<url>`.

**`if curl ... | tail` يقيس `tail` لا `curl`.** **لا تضع أنبوبًا في شرط.**

**قِس حجم النقل لا الحجم الخام.** قياس CSS خامًا أعطى 120 KB وقاد لتوصية
بتقسيمه؛ المضغوط الحقيقي **17 KB** والتقسيم كان سيضرّ.

**`cd X` بلا `|| exit` كارثة.** المجلد اختفى بين الجلسات فنُفِّذت أوامر Git
في مستودع الموقع وأنشأت كوميتة دخيلة.

**`set -e` لا يوقف كل شيء.** **افصل التحقق عن الدفع:** `check && push`.

### ج) الأدوات والصيغ

**HTTP/2 يُصغّر أسماء الترويسات.** `dict(r.headers)` يفقد عدم الحساسية لحالة
الأحرف فيقرأ 301 سليمة كـ«بلا وجهة».

**TOML: أي مفتاح بعد ترويسة جدول ينتمي إليه.** `disableKinds` بعد
`[permalinks]` صار `permalinks.disableKinds` وأسقط البناء **صامتًا**.

**Hugo يضع موارد الحزمة في مجلد اللغة الافتراضية وحده.** **استخدم `.Resources.GetMatch`.**

**لا تكتب مسارات الأصول يدويًّا.** `/blog/css/...` يكسر أي معاينة لا تطابق
الإنتاج. **استخدم `relURL`.**

**‏CSS المدونة محصور تحت `.blog-body`.** أي محدّد يبدأ بعنصر عارٍ
(`html`/`body`/`main`) يعيد تنسيق هيكل الموقع ويزيح الصفحة كلها.

**‏CSS/JS عبر `assets/` مع `fingerprint` لا عبر `static/`.** الخادم يفرض
`immutable`، فاسم ملف ثابت لا يُحدَّث في المتصفح أبدًا مهما نشرت.

**‏`.htaccess` في مجلد فرعي: `RewriteEngine On` يُعيد ضبط سياق التحويل.**
قواعد الجذر لا تُورَّث، فالقواعد تُكرَّر في كل مجلد لغة.

### د) GitHub

**`repository_dispatch` يتطلب `contents:write`** — أي صلاحية تعديل القوالب.
**`workflow_dispatch` يحتاج `actions:write` وحدها.** أُثبت: 403 مقابل 204.

**حماية الفروع مجانية على المستودعات العامة فقط.** اختيار «عام» هو ما يجعل
حماية الفرع قابلة للتنفيذ بلا اشتراك.

**Codeberg تقيّد مرايا السحب التلقائية.** الحل: سير عمل يدفع إليها.

**التوكنات الدقيقة تنتهي صلاحيتها** (سنة كحد أقصى). حين تنتهي يفشل النشر عند
خطوة الدفع برسالة غير واضحة. **سجّل تاريخ الانتهاء.**

### هـ) في الخطة نفسها

- **`Sveltia` لا تدعم فروع المسودات** — أُثبت. آلية `draft` في Hugo هي البديل.
- **هذا الموقع عربي في الجذر** — عكس المدونة المرجعية، وهو ما فرض قلب
  الافتراضات داخل الفاحصات لا في `hugo.toml` وحده.
- **بادئة المسار أثرها على الترتيب صفر** — المعيار هو اتساق التجربة.
- **صفحات `_index.*` ليست مقالات** — مدقّق يعاملها كمقال يُفشل كل بناء.

---

## 4. 📋 المهام المتبقية — مرتّبة بالتبعية

الرموز: 🧑 المالك حصرًا · 🤖 يمكن أتمتته · ⛔ يحجب ما بعده

### 🔴 المرحلة أ — إنشاء المستودعات (تحجب كل ما بعدها)

**[A1] أنشئ مستودع القوالب** · 🧑 ⛔
من حساب `zeroone01z21-alt`، مستودع **عام** باسم مقترح `datarecovery-blog-build`.
ادفع إليه محتوى `~/Downloads/datarecovery-blog-build`.
> «عام» مقصود: حماية الفرع مجانية على العامة فقط.

**[A2] أنشئ مستودع المحتوى** · 🧑 ⛔
من الحساب نفسه، **عام** باسم مقترح `datarecovery-blog-content`.
ادفع إليه محتوى `~/Downloads/datarecovery-blog-content` (فيه مقال بذرة واحد).

**[A3] استبدل الـplaceholders** · 🤖
بعد ثبوت الاسمين، استبدل في مستودع القوالب:
لم يعد هناك placeholder — المستودعان منشآن، و`blog-deploy` متقاعد فلا مقابل له.
> مملوءان مسبقًا ولا يحتاجان تغييرًا: مستودع الموقع `01team9639-maker/datarecovery-sa`،
> ومالك مستودعات المدونة `zeroone01z21-alt`.

### 🔴 المرحلة ب — الصلاحيات

**[B1] مفتاح صلاحية لمستودع الموقع** · 🧑 ⛔
من حساب **`01team9639-maker`** (مالك مستودع الموقع، لا حساب المدونة):
`Settings → Developer settings → Personal access tokens → Fine-grained`
- Resource owner: `01team9639-maker`
- Repository access: **Only select repositories → `datarecovery-sa`** فقط
- Permissions: **Contents = Read and write**

ثم في مستودع القوالب: `Settings → Secrets and variables → Actions`
أضف `SITE_REPO_TOKEN` بقيمة المفتاح الجديد.
> **لا تُعِد استخدام مفتاح المشروع الشقيق.** مفتاح محصور بمستودع واحد لا
> يستطيع الكتابة على الآخر حتى لو تسرّب. الكلفة صفر.

**[B2] حماية الفرع** · 🧑
على مستودعي المدونة: `Settings → Branches → Add rule` على `main`.

**[B3] ادعُ الكاتب** · 🧑
صلاحية **Write على مستودع المحتوى وحده**. لا تدعوه إلى مستودع القوالب.

### 🟠 المرحلة ج — لوحة الكاتب

**[C1] أضف النطاق إلى وسيط الدخول** · 🧑
في Cloudflare، على الـWorker القائم: أضف `datarecovery-sa.com` إلى
`ALLOWED_DOMAINS` (قائمة مفصولة بفواصل). **لا حاجة لـWorker ثانٍ** — تحقّقتُ
من الكود، يقبل عدة نطاقات.

**[C2] عنوان المعاينة** · 🧑
نطاق فرعي من لوحة الاستضافة + حساب FTP محصور فيه، ثم أضف الأسرار:
`PREVIEW_FTP_HOST` · `PREVIEW_FTP_USER` · `PREVIEW_FTP_PASSWORD` · `PREVIEW_FTP_DIR`

### 🟡 المرحلة د — قرار القياس

**[D1] عالج ازدواج عدّ الزيارات** · 🧑 قرار + 🤖 تنفيذ
`layouts/partials/analytics.html` ينادي `gtag('config')` **و** يحمّل `gtm.js`،
والموقع يعرّف GA4 **كوسم داخل الحاوية** — فتُحتسب `page_view` مرتين على المدونة.

الخياران:
1. **عدّل الجزئية** لتحمّل GTM وحده وتترك ضبط GA4 للحاوية — لا يمسّ الموقع. *(موصى به)*
2. **احذف وسم GA4 من الحاوية** — يمسّ قياس الموقع كله.

### 🟢 المرحلة هـ — بعد أول نشر

**[E1] Search Console** · 🧑 — أضف `sitemap.xml` وتحقّق من التغطية.
**[E2] مراقب خارجي** · 🧑 — اربط `HEALTHCHECKS_URL`.
**[E3] مرآة Codeberg** · 🧑 — `CODEBERG_TOKEN` إن أردت النسخ الاحتياطي.
**[E4] خطة المحتوى** · 🧑 — من يكتب، وبأي وتيرة، وأي مواضيع.
> المقال البذرة `backup-3-2-1-rule` صالح للنشر، لكنه واحد. المدونة بلا وتيرة
> نشر منتظمة لا تعطي أثرًا في البحث.

---

## 5. ما لا يجب توقّعه

- **المدونة ليست على الإنترنت** ولن تكون قبل إتمام المرحلتين أ وب.
- **مقال واحد فقط** موجود، وهو للاختبار.
- **أثر البحث تراكمي** — يظهر بعد أشهر من النشر المنتظم لا بعد أسابيع.
