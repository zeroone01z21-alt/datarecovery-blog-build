# سجل القرارات — مدونة datarecovery-sa.com

**النسخة 1.0 · 2026-08-16 · المدونة حيّة**

> مصدر الحقيقة لهذا المشروع. ما ليس مذكورًا هنا لم يُتفق عليه.
>
> هذا السجل **مشتق** من سجل المشروع الشقيق (`blog-decisions-register-v2.2.md`)
> لكنه ليس نسخة منه: بعض قراراته ثبت خطؤها عمليًّا، وبعضها لا ينطبق على موقع
> بنيته مختلفة. القسم 1 يحصر الفروق.

---

## 0 · الحالة

| | |
|---|---|
| المدونة | حيّة على `datarecovery-sa.com/blog/` منذ 2026-08-16 |
| اللغات | عربية على `/blog/` · إنجليزية على `/blog/en/` |
| البوابات | ثماني، كلها إلزامية قبل النشر |
| المحتوى | مقال بذرة واحد — خطة المحتوى لم تُقرّر بعد |
| الكاتب | لم يُدعَ بعد |

---

## 1 · قرارات المشروع الشقيق التي **لا** تنطبق هنا

أخطر ما في إعادة استخدام سجل جاهز أن يُنقل قرار متقاعد. هذه أربعة ثبت خطؤها
هناك **بعد** كتابة سجلهم، فلا تُحيا:

| قرارهم الأصلي | ما استقرّ عليه فعلًا | الدليل |
|---|---|---|
| `repository_dispatch` لتشغيل البناء | **`workflow_dispatch`** | يتطلب الأول `contents:write` أي القدرة على تعديل القوالب والميزانيات. أُثبت 2026-08-02: 403 مقابل 204 |
| مستودعات **خاصة** | **عامة** | حماية الفروع مجانية على العامة فقط؛ الخاصة تحتاج اشتراكًا. والمستودعات بلا أسرار أصلًا |
| مستودع نشر ثالث `blog-deploy` | **متقاعد** | النشر يتم داخل مستودع الموقع. المستودع الثالث طبقة بلا فائدة |
| سقف 2000 دقيقة بناء شهريًّا | **بلا حد** | السقف يخصّ المستودعات الخاصة. حساب السعة في سجلهم مبني على فرضية ساقطة |

**قرار «بند صفر» عندهم — اتجاه اللغة — كان حاجبًا للمشروع كله. عندنا لا يوجد:**
الموقع عربي على جذر النطاق، فالمدونة عربية على `/blog/`. المنطق متسق داخل
النطاق ولا يحتاج قرار مالك.

---

## 2 · القرارات المعمارية

### 2.1 التوليد وقت البناء

كل صفحة HTML كاملة قبل وصول الزائر. لا جافاسكربت يجلب محتوى، ولا قاعدة بيانات،
ولا طلب خارجي وقت التصفح. الزائر ومحرك البحث ومعاينة الروابط يستقبلون الشيء نفسه.

### 2.2 ترتيب اللغات يتبع الموقع

```
العربية  → /blog/        (اللغة الافتراضية، بلا بادئة)
الإنجليزية → /blog/en/
```

**تحذير صيانة:** قلب الترتيب عن المدونة المرجعية لامس أكثر من `hugo.toml` بكثير.
`check_seo.py` (هدف x-default، واشتقاق اللغة من المسار، وزوج `.htaccess`) و
`check_indexing.py` (مواضع الخرائط وRSS) و`normalize_sitemaps.py` (كان يحذف
`public/en/` باعتباره مجلدًا أثريًّا — عندنا هو **موقع الإنجليزية كامل**) و
`check_budgets.py`. أي عودة إلى القلب تلمسها كلها.

### 2.3 الهيكل يُصدَّر من المولّد لا يُكشط من HTML

المدونة المرجعية تستخرج الهيدر والفوتر من صفحة مُصيَّرة لأن موقعها مكتوب يدويًّا.
صفحات هذا الموقع **مولّدة** من `build/generate.js`، فالهيكل يُقرأ من مصدره عبر
`build/emit-chrome.js`.

لا تحليل HTML، ولا عدّ وسوم `<div>` لمعرفة أين تنتهي كتلة، ولا انحراف صامت عند
تغيّر الترميز. يتغيّر رقم الهاتف في الموقع فتتبعه المدونة في النشر التالي.

### 2.4 المدونة تحمّل `main.js` وحده

`bootstrap.js` يضيف `.js` إلى `<html>`، وهو ما يفعّل قواعد مثل
`.js .footer__bottom { opacity: 0 }` التي يكشفها GSAP على الموقع. المدونة بلا
طبقة حركة، فتحميله **يخفي الفوتر نهائيًّا**. واستبعاده يوفّر ~115 KB.

### 2.5 الخط والألوان من الموقع، لا نسخة منهما

Alexandria يأتي من `fonts.css` الموقع الذي يحمّله الهيكل أصلًا. المدونة لا تشحن
خطًّا خاصًّا بها.

> **درس مدفوع الثمن:** شُحنت المدونة أول مرة بخطوط المشروع الشقيق (Cairo و
> Dennis Sans، 102 KB) وألوانه (كريمي وبرتقالي). النتيجة: صفحة تنزّل مجموعتَي
> خطوط، ونصفها الأعلى بهوية ونصفها الأسفل بأخرى. أُصلح 2026-08-16.

---

## 3 · البنية والصلاحيات

| المستودع | المحتوى | من يصل |
|---|---|---|
| `zeroone01z21-alt/datarecovery-blog-content` | Markdown وصور فقط | الكاتب (Write) + المالك |
| `zeroone01z21-alt/datarecovery-blog-build` | القوالب والبوابات والأسرار | المالك وحده |
| `01team9639-maker/datarecovery-sa` | الموقع، والمدونة تحت `blog/` | المالك · تسحبه Hostinger |

**لماذا الفصل:** مستودع المحتوى لا يحوي منطق بناء ولا أسرارًا ذات قيمة. اختراق
حساب الكاتب لا يمنح المهاجم خط بناء يعدّله؛ أقصى ضرر ممكن مقال سيّئ وتشغيل بناء
عبثي، وكلاهما يُتراجع عنه بدقيقة.

### الأسرار

| السر | مكانه | صلاحيته | أقصى ضرر لو تسرّب |
|---|---|---|---|
| `BUILD_TRIGGER_TOKEN` | المحتوى | Actions على القوالب وحدها | تشغيل بناء |
| `SITE_REPO_TOKEN` | القوالب | Contents على `datarecovery-sa` وحده | كتابة في الموقع |
| `PREVIEW_FTP_*` | القوالب | مجلد المعاينة وحده | العبث بصفحة معاينة |

**مفتاحان من حسابين مختلفين عمدًا.** لو كان مفتاحًا واحدًا لكان تسريب أيّهما
يعني الاثنين. وكلاهما ينتهي خلال سنة — سجّل تاريخي الانتهاء.

---

## 4 · النشر

```
الكاتب يحفظ → dispatch.yml يرسل إشارة → البناء → ثماني بوابات
→ دفع blog/ داخل مستودع الموقع → مزامنة Git في Hostinger → الزائر
```

**مزامنة Git تنشر المستودع كاملًا** إلى `public_html`، لا قائمة سماح. فالمصادر
(`build/` و`tests/` و`design/`) تصير على الخادم محجوبةً بـ`.htaccess`.

الوضع انتقل من «المصدر غير موجود» إلى «موجود ومحجوب بملف». الأول أقوى، والثاني
مقبول **بشرط الفحص**: `tests/git-sync-safety.sh` يشغّل Apache على المستودع كاملًا
ويؤكد أن كل مصدر محجوب. **شغّله بعد أي تعديل على `.htaccess`.**

> **لا تحذّر من أن المزامنة تمحو المجلدات التي لم تنشئها.** أُثبت عكسه:
> `preview.zero2one.sa` مجلده داخل `public_html` وليس في المستودع، ونجا من
> مزامنة جرت في اليوم السابق.

---

## 5 · البوابات الثماني

| البوابة | ما تمنعه |
|---|---|
| `check_controls` | تعديل ملف ضوابط بلا تسجيل بصمته |
| `check_content` | مقال ناقص حقلًا أو متجاوزًا حدًّا |
| `check_cms` | انحراف اللوحة عن المخطط |
| `check_seo` | عنوان أو وصف أو canonical أو hreflang ناقص · أكثر من `h1` |
| `check_indexing` | خريطة أو RSS أو IndexNow معطوب |
| `check_internal_links` | **رابط مكسور إلى صفحة خدمة** |
| `check_budgets` | تآكل الأداء |
| `check_deploy_scope` | كتابة خارج `blog/` |

`check_internal_links` **إضافة خاصة بهذا المشروع**: `check_seo` يفحص الروابط
داخل `/blog/` فقط، فرابط إلى `/services/hdd.html` يخرج من نطاقه. وخطأ مطبعي واحد
يعطي الزائر 404 في اللحظة التي كان فيها أقرب ما يكون إلى طلب الخدمة.

---

## 6 · المحتوى

- **الروابط بلا تاريخ وبلا تصنيف.** التاريخ يجعل المقال يبدو قديمًا، والتصنيف
  يكسر الرابط عند إعادة التصنيف. **الرابط بعد النشر لا يتغيّر.**
- **الأرقام إنجليزية** في كل مكان حتى داخل العربية. قاعدة موقعية، ويوجد اختبار
  يمنع خرقها.
- **لا حذف من اللوحة** — أرشفة فقط. الحذف النهائي إجراء يدوي.
- **لا ترجمة آلية.** الكاتب يكتب النسختين أو ينشر بلغة واحدة.
- **`heading-one` مستبعد** من شريط المحرر: القالب يصيّر العنوان `h1`، وعنوان
  ثانٍ في المتن يُفشل البناء برسالة لا يفهم الكاتب سببها.
- **الروابط الداخلية نسبية.** المطلقة تعمل في الإنتاج وتكسر في المعاينة.

### ادعاءات ممنوعة

لا أسعار، ولا ضمانات، ولا نسب نجاح، ولا مدد إنجاز، ولا شهادات، ولا أسماء عملاء،
ولا آراء عملاء، ولا إحصاءات عن حجم العمل. **ولا عبارة «لا استعادة، لا رسوم».**

مؤكَّد ومسموح: فحص وتشخيص مجاني بلا التزام · توقيع NDA عند الطلب · **المختبر في
الرياض ولا فروع في مدن أخرى**.

---

## 7 · القياس

الموقع والمدونة والمقالات يحمّلون **حاوية GTM واحدة** من ملف واحد:
`assets/js/analytics.js` على الموقع، يشحنه `build/emit-chrome.js` إلى المدونة
ضمن `site-scripts.html`. وGA4 (`G-M2GX0NVW5E`) مُعرَّف **كوسم داخل الحاوية**،
فلا يُنادى `gtag('config')` في أي صفحة، ويُحتسب `page_view` مرة واحدة.

**حُسم في 2026-08-18** بطلب المالك: قياس موحّد على كل صفحات النطاق، بنفس
الحاوية ونفس توقيت التحميل، كي تكون أرقام النصفين قابلة للمقارنة.

وكشف التنفيذ عطلًا لم يرصده أحد. الجزئية القديمة
`layouts/partials/analytics.html` كانت تكتب `<script>` **مضمّنًا**، وسياسة
الأمان على النطاق كله `script-src 'self' https://www.googletagmanager.com`
بلا `'unsafe-inline'`. فكان المتصفّح يحجب السكربت، و**المدونة بلا أي قياس
منذ إطلاقها** — لا عدّ مزدوج، بل لا عدّ إطلاقًا. حُذفت الجزئية.

> الدرس: قراءة الكود تُثبت النيّة لا الأثر. رأينا نداءين فاستنتجنا عدًّا
> مزدوجًا، والحقيقة أن أيًّا منهما لم يُنفَّذ قط. افحص الترويسة الحيّة قبل
> تشخيص سلوك المتصفّح.

**أثر جانبي مقبول:** المدونة صارت تجلب GTM فورًا (`async`) بدل الانتظار حتى
الخمول أو أول تفاعل. يكلّف ذلك نقطة أو نقطتين في Lighthouse على صفحات المدونة
— وهي الكلفة نفسها التي يدفعها الموقع أصلًا — مقابل عدم إسقاط الزيارات
القصيرة التي كانت تنتهي قبل تحميل السكربت.

---

## 8 · قرارات مفتوحة

| # | القرار | يحجب |
|---|---|---|
| 🔴 1 | **مراجعة بشرية قبل نشر الكاتب؟** سجل المشروع الشقيق يفرضها؛ إعدادنا الحالي يسمح بالنشر مباشرة بعد اجتياز البوابات الثماني | إتمام هذا السجل |
| 🟡 2 | **وتيرة النشر ومن يكتب** — كم مقالًا شهريًّا، والكاتب أم مساعد برمجي بمراجعة | مقالات الإطلاق |

**لا يُغلق أيّ منها بالافتراض.** حين يُحسم، يُدوَّن هنا بسببه وتاريخه.

---

## 9 · بنود تشغيلية متبقية

- **حماية كلمة مرور على `preview.datarecovery-sa.com`** — سير عمل المعاينة
  **يسحب** `.htaccess` القائم ويشترط بقاء Basic Auth فيه، فيفشل بدونها.
  ⚠️ **لا تُستخدم أداة `Password Protect Directories` في hPanel** — أسقطت خادم
  المشروع الشقيق بالكامل في 2026-08-03. استخدم `.htpasswd` يدويًّا.
- **`ALLOWED_DOMAINS`** في Worker `sveltia-cms-auth` المشترك: أضف نطاق معاينتنا
  بفاصلة. لا Worker ثانٍ ولا تطبيق OAuth ثانٍ — الوسيط يقبل قائمة.
- **دعوة الكاتب** على مستودع المحتوى وحده، بتحقق ثنائي ورموز استرداد محفوظة.
- **حماية الفروع** على المستودعين.

---

## ملحق · أسئلة لا تُطرح ثانيةً

| السؤال | الجواب |
|---|---|
| لماذا Hugo لا امتداد للمولّد الحالي؟ | المدونة تحتاج تصنيفات وترقيم صفحات وصورًا متجاوبة وخرائط لكل لغة وRSS وhreflang وبيانات منظّمة. التوسيع = كتابة مولّد مواقع من الصفر وصيانته وحدنا |
| لماذا لا مستودع نشر ثالث؟ | متقاعد — القسم 1 |
| لماذا `/blog/en/` لا `/en/blog/`؟ | الثاني يتطلب مدى نشر يلامس جذر النطاق. أثر المسار على SEO **صفر**: إشارة اللغة من `hreflang` و`lang` والمحتوى |
| لماذا لا فرع للمسودات؟ | Sveltia لا تدعم Editorial Workflow — **أُثبت عمليًّا**. البديل `draft` في Hugo |
| لماذا لا ترجمة آلية؟ | القاموس الثابت لا يغطي نصوصًا طويلة، والنتيجة تُقرأ كترجمة |
| لماذا الأرقام إنجليزية؟ | قرار موقعي شامل، ويوجد اختبار يفرضه |
