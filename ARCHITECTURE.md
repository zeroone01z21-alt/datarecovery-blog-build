# البنية — مدونة من الصفر إلى الواحد

> العقد الحالي بين المحتوى والبناء والموقع. هذا الملف يصف ما يعمل فعليًا، لا
> مسار SFTP القديم في خطة التنفيذ.

آخر تحديث: 2026-08-05 · الحالة: **المدونة الإنتاجية حية. وسيط OAuth للوحة
الكاتب منشور ومربوط؛ أول رفع للمعاينة ينتظر أسرار FTP المحصورة من المالك.**

## 1. الخريطة

```text
الكاتب
  │ حفظ Markdown وصور
  ▼
┌────────────────────┐  workflow_dispatch  ┌────────────────────┐
│ blog-content       │ ──────────────────▶ │ blog-build         │
│ مصدر المقالات      │ BUILD_TRIGGER_TOKEN │ Hugo + قوالب       │
│ وحزم الصور         │ actions:write فقط   │ بوابات + ميزانيات  │
└────────────────────┘                     └─────────┬──────────┘
                                                    │ SITE_REPO_TOKEN
                                                    ▼
                                          ┌──────────────────────┐
                                          │ datarecovery-sa         │
                                          │ الموقع + blog/ مولّد │
                                          └─────────┬────────────┘
                                                    │ Git sync على main
                                                    ▼
                                          datarecovery-sa.com/blog/
                                                    │ بعد ظهور علامة النشر
                                                    └──▶ IndexNow
```

الفصل الحقيقي ثنائي للمصدر (`blog-content` و`blog-build`) ثم تسليم محدود إلى
مستودع الموقع. `blog-deploy` ليس طبقة ثالثة نشطة؛ هو مستودع عام فارغ ومتقاعد.

هذا المسار يخالف عزل المستودعات المثالي لأن خط المدونة يملك رمز كتابة على
مستودع الموقع. السبب العملي: Hostinger تزامن مستودعًا واحدًا إلى `public_html`
وتحذف ما لا يوجد فيه. عندما كان `/blog/` يُرفع منفصلًا بـFTP، حذفته مزامنة
الموقع في 2026-08-04. التعويض الحالي هو حارس diff إلزامي قبل كل push.

## 2. المستودعات والأدوار

| المستودع | المالك | الدور الحالي | الكتابة المعتادة |
|---|---|---|---|
| `zeroone01z21-alt/datarecovery-blog-content` | حساب المدونة | Markdown وصور فقط | الكاتب والمالك |
| `zeroone01z21-alt/datarecovery-blog-build` | حساب المدونة | Hugo، قوالب، بوابات، Actions | المالك/مراجعة محمية |
| `01team9639-maker/datarecovery-sa` | حساب الموقع | 42 صفحة رئيسية + `blog/` المولّد | خط الموقع وخط المدونة المحدود |
|  مستودع نشر ثالث | حساب المدونة | **متقاعد وفارغ** | لا أحد |

كلها عامة بقرار المالك. الكاتب يستطيع رؤية `blog-build` لأنه عام، لكنه لا يملك
صلاحية الكتابة عليه؛ يملك Write على `blog-content` فقط.

إذا دخلت بيانات عميل أو نتائج غير منشورة في المقالات، يجب إعادة تقييم رؤية
`blog-content` قبل إدخالها. تاريخ Git العام دائم حتى لو حُذفت لاحقًا.

## 3. عقد المحتوى

المصدر الملزم هو `controls/schema.json`. يقرأه `tools/check_content.py` قبل
كل بناء، ويولّد منه `tools/generate_cms.py` إعداد Sveltia ودليل الكاتب. يمنع
`tools/check_cms.py` انجراف الحقول أو نسخة Sveltia أو إعادة زر الحذف.

البنية:

```text
blog-content/content/
├── _index.en.md
├── _index.ar.md
└── blog/<slug>/
    ├── index.en.md
    ├── index.ar.md        # اختيارية؛ المقال أحادي اللغة مسموح
    ├── cover.webp
    └── *.webp
```

الحقول الأساسية: `title`, `description`, `slug`, `date`, `draft`,
`categories`, `featured_image`, `featured_image_alt`. الحدود الدقيقة ورسائل
الخطأ العربية والتصنيفات الستة في JSON، لا في هذا المستند.

قواعد لا تتغير:

- الصور داخل حزمة المقال، لا مكتبة صور عامة.
- لا HTML خام في Markdown.
- لا ترجمة آلية.
- slug المنشور ثابت؛ الحذف التحريري يتحول إلى أرشفة ما لم يقرر المالك خلافه.
- `archived: true` يستبعد الحزمة كلها من HTML والخرائط وRSS، ويبقيها في Git.
- قيم `slug` و`archived` و`categories` واحدة بين ترجمات الحزمة.
- ملفات `_index.*` صفحات قوائم وليست مقالات، لذلك لا يطبق عليها مدقق المقالات.

في خطوة التجهيز، تنسخ `tools/prepare_content.py` الحزم النشطة من
`content/blog/` إلى جذر محتوى Hugo؛ لأن `baseURL` يحوي `/blog/` أصلًا، إبقاء
قسم اسمه `blog` سيولد `/blog/blog/`. الأداة ترفض الروابط الرمزية ولا تنظف
مجلدًا لم تنشئه هي بعلامة واضحة.

التصنيفات الستة قائمة مركزية ثابتة، لا مجموعة قابلة للتحرير في مستودع
المحتوى. هذا تصحيح متعمد لمطلب الخطة التاريخي ذي مجموعتي `posts/categories`:
السماح للكاتب بإنشاء تصنيف كان سيصنع مصدر حقيقة ثانيًا وصفحات SEO رقيقة بلا
وصف. اللوحة تعرض التصنيفات كاختيار متعدد مولّد من العقد، وأي تصنيف جديد يمر
كتغيير مخطط وقوالب مراجع.

## 4. الروابط واللغة

| المخرج | الإنجليزية | العربية |
|---|---|---|
| الرئيسية | `/blog/` | `/blog/en/` |
| المقال | `/blog/{slug}/` | `/blog/en/{slug}/` |
| التصنيف | `/blog/categories/{slug}/` | `/blog/en/categories/{slug}/` |
| RSS | `/blog/index.xml` | `/blog/en/index.xml` |
| خريطة اللغة | `/blog/sitemap-en.xml` | `/blog/sitemap-ar.xml` |

Hugo يولّد خرائط اللغات أولًا في مجلدات داخلية، ثم ينقلها
`tools/normalize_sitemaps.py` إلى جذر `/blog/`. هذا إلزامي لأن خريطة داخل
`/blog/en/` لا تملك نطاقًا يصف روابط `/blog/` الأب. `disableDefaultSiteRedirect = true` يمنع توليد
`/blog/en/index.html` كتحويل JavaScript ناقص metadata. الإنجليزية أصلًا في
الجذر، والعربية وحدها ذات بادئة. Apache يعيد `/blog/en/` إلى `/blog/` بـ301.

كل صفحة مترجمة تعلن `hreflang` متبادلًا، و`x-default` يشير للإنجليزية.
الروابط بلا تاريخ وبلا تصنيف حتى لا تنكسر عند تحديث المقال أو إعادة تصنيفه.

## 5. البناء والنشر

`build.yml` ينفذ 23 مرحلة ظاهرة تشمل checkout، تثبيت SHA المحتوى، بصمات
الضوابط، عقد CMS، تجهيز
المحتوى واستبعاد المؤرشف، Hugo المثبت بالبصمة، الفهرسة وSEO والميزانيات، حارس
نطاق الموقع، push، تحققًا حيًا إلزاميًا، IndexNow، إشارة مراقبة اختيارية، وملخصًا
ثنائي اللغة. العدد يتضمن إشارات النجاح/الفشل والملخص، لا بوابات مستقلة كلها.

بناء الإنتاج والتراجع يشتركان في `concurrency.group = blog-site-write` مع
`queue: max` و`cancel-in-progress = false`: لا يمكن لتشغيل جديد إلغاء تشغيل
جارٍ أو استبدال تشغيل منتظر قبل التحقق. مستودع المحتوى ينتظر 30 ثانية قبل الإشارة ويلغي الإشارة الأقدم،
فيجمع الحفظات السريعة قبل أن تدخل طابور الإنتاج. القياس القديم 10→5 يخص
الإعداد السابق القابل للإلغاء، ولا يُنسب إلى التصميم الحالي قبل إعادة قياسه.
كل إشارة ترسل SHA كاملًا؛ checkout المحتوى يثبت هذا الـcommit نفسه، ثم يعيد
Workflow المحتوى نتيجة التشغيل المرتبط به للكاتب بدل بناء نسخة أحدث صامتًا.

إدخال `reason` من `workflow_dispatch` يمر عبر متغير بيئة قبل أمر Git؛ لا يوضع
GitHub expression مباشرة في shell. السبب أمان مهم لأن خطوة الدفع تحمل
`SITE_REPO_TOKEN`.

### لماذا IndexNow غير حاجب للنشر

كل البوابات حتى الدفع حاجبة. بعد push قد تتعطل خدمة IndexNow؛ لهذا خطوة
الإخطار `continue-on-error`. أما ظهور علامة النشر الحية نفسها فحاجب: فشلها
يجعل الحالة «غير مؤكدة» ويطلب تحقق المالك. HTML والخرائط تبقى قابلة للاكتشاف
حتى لو تعطل IndexNow، ويظهر ذلك في ملخص Actions.

الأداة لا تنتظر مجرد وجود مفتاح قديم. تكتب `indexnow-deploy.txt` بمعرّف فريد،
ثم لا ترسل الإخطار حتى يظهر **المعرّف نفسه** على الموقع. كل نشر يرسل جرد الخرائط
الحالي مع الروابط المحذوفة. لا يُلغى تشغيل الإنتاج بعد دخوله طابور الكتابة،
حتى لا يبقى push بلا تحقق حي أو إخطار.

## 6. الفهرسة

Hugo يولّد البنية، ثم تطبّع خطوة ما بعد البناء مواقع الخرائط إلى:

- `/blog/sitemap.xml`: فهرس جامع.
- `/blog/sitemap-en.xml` و`/blog/sitemap-ar.xml`: خريطة لكل لغة في أعلى
  مجلد مشترك مع كل الروابط التي تصفها.
- `/blog/index.xml` و`/blog/en/index.xml`: RSS مترجم لكل لغة.

`tools/check_indexing.py` يثبت قبل النشر:

- وجود المخرجات الخمسة وصحة XML.
- أن فهرس الخرائط يشير إلى اللغتين فقط.
- أن الروابط ضمن `/blog/` ومسارات اللغة صحيحة.
- أن `hreflang` متبادل بين الخرائط.
- أن عناصر RSS موجودة في خريطة لغتها.
- أن صفحة التحويل `/blog/en/` غير مولدة.
- أن ملف مفتاح IndexNow العام وصل إلى المخرج وتطابق مع المصدر.

`robots.txt` في جذر الموقع لا يولده Hugo لأن المدونة لا تملك الجذر. أُضيف إلى
مستودع الموقع السطر:

```text
Sitemap: https://datarecovery-sa.com/blog/sitemap.xml
```

## 7. الأمان والضوابط

| الطبقة | الآلية | الحد الحقيقي |
|---|---|---|
| فصل المصدر | الكاتب يكتب في `blog-content` فقط | لا يعدل workflow أو بوابات `blog-build`؛ يستطيع تقنيًا تعديل workflow المحتوى، والرمز هناك محدود بـActions على مستودع البناء |
| رمز التشغيل | `BUILD_TRIGGER_TOKEN`: `actions:write` على `blog-build` | يشغّل workflow ولا يكتب القوالب |
| رمز الموقع | `SITE_REPO_TOKEN`: `contents:write` على `datarecovery-sa` | **ليس محصورًا بمجلد**؛ الحارس تعويض |
| حارس النطاق | `check_deploy_scope.py` | يفشل لأي diff خارج `blog/` |
| بصمات | `check_controls.py` — workflows والعقود والبوابات الحساسة | تكشف التغيير المقصود؛ لا تمنعه وحدها |
| حماية الفرع | CODEOWNERS/قواعد GitHub | الطبقة التي تجعل مراجعة الضوابط إلزامية |
| محتوى | Goldmark `unsafe = false` + مدقق schema | يمنع HTML الخام والنشر الناقص |
| إصدارات | Hugo وActions مثبتة بإصدار/SHA | يمنع انجراف الاعتماديات الصامت |

البصمة «تلاعب مكشوف» لا «تلاعب مستحيل»: من يملك الكتابة يستطيع تعديل الملف
وبصمته معًا، لكن الـdiff يكشف القرار. حماية الفرع والمراجعة هما حد المنع.

حُذفت أسرار FTP الإنتاجية الأربعة الميتة من `blog-build`. حساب
`u497805864.blogdeploy` ميت ويزيله المالك (T4). حساب `previewdeploy` يبقى
للمعاينة. الرمزان `BUILD_TRIGGER_TOKEN` و`SITE_REPO_TOKEN` يحتاجان
تدويرًا عاجلًا لأن قيمتيهما ظهرتا سابقًا في محادثة (T6b).

## 8. لوحة الكاتب والمعاينة

- Sveltia CMS v0.179.0 مستضافة ذاتيًا في `/preview/admin/` ببصمة SHA-256 وSRI.
- اللوحة تكتب `index.en.md` و`index.ar.md` في `blog-content:main`، بلا حذف أو
  نسخ، وتضغط الصور إلى WebP حتى 1600×1600 قبل بوابة 500KB.
- `preview.yml` يبني المسودات على `https://preview.datarecovery-sa.com/blog/` ويرفع
  الجذر والمدونة واللوحة عبر حساب FTP المحصور، وكل HTML فيه `noindex`.
- الرفع يحافظ على Basic Auth: يسحب `.htaccess` العامل، يثبت وجود
  `AuthType Basic` و`AuthUserFile` و`Require valid-user`، يزيل `Header` القديمة،
  ويعيده بعد الحمولة. `.htpasswd` لا يدخل الحذف أو الحمولة.
- حارس النشر يرفض OAuth placeholder أو أي سر مفقود من `PREVIEW_FTP_*`. OAuth
  مضبوط حاليًا، وتبقى أسرار FTP الأربعة وأول اختبار حي قبل وصف اللوحة بأنها حية.

## 9. الأرقام المقيسة

آخر بناء نظيف محلي بـHugo 0.164.0 ومحتوى الإثبات:

| القياس | النتيجة | الحد |
|---|---:|---:|
| CSS مضغوط Brotli | 2.6 KB | 20 KB |
| خطوط عربية | 56.4 KB | 110 KB |
| خطوط إنجليزية | 45.9 KB | 110 KB |
| أثقل صورة | 27.7 KB | 500 KB |
| أثقل مسار حرج | 78.0 KB | 220 KB |

بناء Actions الكامل قيس سابقًا بنحو 50 ثانية. بعد دمج المدونة، تدقيق الموقع
يمر على 42 صفحة HTML (22 للموقع + 20 للمدونة) بلا ERROR أو WARN. بناء Hugo
الحالي ينتج EN 20 / AR 19 داخليًا، منها صفحات التصنيفات و404 والخرائط/RSS.

## 10. التراجع والنسخ الاحتياطي

`rollback.yml` يأخذ SHA كاملًا من تاريخ `datarecovery-sa/main`، يثبت أنه ancestor،
يستخرج `blog/` منه، ويصنع commit تراجع جديدًا بدل reset. بعد الحارس والدفع
ينتظر علامة التراجع الحية. يتسلسل مع الإنتاج في مجموعة الكتابة نفسها.

`mirror.yml` ينسخ أسبوعيًا `blog-content` و`blog-build` و`blog-deploy` إلى
Codeberg؛ الثالث إرث فارغ إلى أن يقرر المالك حذفه من المصفوفة والمستودعات.
هذه المرآة لا تشمل `datarecovery-sa`. لذلك ينسخ `tools/backup_local.py` شهريًا
المستودعات الفعلية الثلاثة كـGit mirrors، يشغل `git fsck`، ويكتب manifest.
اختُبر الأمر فعليًا على النسخ العامة الثلاث؛ تبقى مسؤولية المالك اختيار قرص
الوجهة ونسخه إلى قرص خارجي كل ربع سنة.

## 11. افتراضات ثبت خطؤها

- SFTP ليس مسار الإنتاج؛ مزامنة الموقع تحذف ما لا يوجد في مستودعه.
- `blog-deploy` ليس مخزن التراجع ولا صورة لما هو حي.
- `repository_dispatch` احتاج صلاحية أوسع؛ المسار العامل هو
  `workflow_dispatch` مع `actions:write`.
- الكاتب يرى المستودع العام، لكن الرؤية ليست صلاحية كتابة.
- Sveltia لا تدعم فروع Editorial Workflow؛ المسودات حقل Hugo.
- الموقع إنجليزي في الجذر، لا عربي.
- حجم CSS الخام ليس ميزانية نقل؛ القياس بعد Brotli.
- نجاح `set -e` أو push لا يثبت وصول Hostinger؛ يلزم تحقق خارجي/علامة نشر.

## 12. التالي

لا توجد مهمة كود إنتاجية معروفة حاجبة بعد هذا التغيير. ما بقي خارجيًا موثق في
`HANDOFF.md`: Search Console، OAuth للوحة، أسرار FTP المعاينة، استرداد الكاتب،
تدوير الرموز، تنظيف حساب FTP، المراقب الخارجي، وقرارات المحتوى/PDPL/ساعات
العمل. لا تنفّذ مهمة مالك بالنيابة عنه دون تفويض صريح لحسابه.
